/*
 *implemente un algoritmo que le permita:
   1). Desplegar el nombre del proyecto de su papaer.
   2). Qué es una red neuronal.
   3). Cuáles son los elementos que forman una red neuronal.
   4). Cómo utilizaron la red neuronal en el proyecto que usted ha seleccionado.
   5). Cómo utilizaria usted la red neuronal como mejoramiento al proyecto.
   6). En que consistio el entrenamiento utiliando una red neuronal, indique el algoritmo utilizado.
 */
package redneuronal1;

import java.util.Scanner;

/**
 *
 * @author Erick Unda
 */
public class RedNeuronal1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        Scanner obj=new Scanner(System. in);
        System.out.println("Redes Neuronales");

        System.out.println("Ingrese su Nombre:");
        String nombre=obj.nextLine();

        System.out.println("¿Cuál es el nombre del Paper?");
        String paper=obj.nextLine();

        System.out.println("Defina una Red Neuronal:");
        String redneuronal=obj.nextLine();

        System.out.println("¿Cuáles son los elementos que forman una red neuronal?");
        String elementos=obj.nextLine();

        System.out.println("¿Cómo utilizaron la red neuronal en el proyecto que usted ha seleccionado?");
        String utilizar=obj.nextLine();



         System.out.println("¿Cómo utilizaria usted la red neuronal como mejoramiento al proyecto?");
         String redneuronal1=obj.nextLine();


          System.out.println("En que consistio el entrenamiento utiliando una red neuronal, indique el algoritmo utilizado");
         String utilizar1=obj.nextLine();


\\fin del proyecto

    }

}
